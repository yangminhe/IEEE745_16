"""
This type stub file was generated by pyright.
"""

from serial.serialutil import SerialBase

LOGGER_LEVELS = ...
SE = ...
NOP = ...
DM = ...
BRK = ...
IP = ...
AO = ...
AYT = ...
EC = ...
EL = ...
GA = ...
SB = ...
WILL = ...
WONT = ...
DO = ...
DONT = ...
IAC = ...
IAC_DOUBLED = ...
BINARY = ...
ECHO = ...
SGA = ...
COM_PORT_OPTION = ...
SET_BAUDRATE = ...
SET_DATASIZE = ...
SET_PARITY = ...
SET_STOPSIZE = ...
SET_CONTROL = ...
NOTIFY_LINESTATE = ...
NOTIFY_MODEMSTATE = ...
FLOWCONTROL_SUSPEND = ...
FLOWCONTROL_RESUME = ...
SET_LINESTATE_MASK = ...
SET_MODEMSTATE_MASK = ...
PURGE_DATA = ...
SERVER_SET_BAUDRATE = ...
SERVER_SET_DATASIZE = ...
SERVER_SET_PARITY = ...
SERVER_SET_STOPSIZE = ...
SERVER_SET_CONTROL = ...
SERVER_NOTIFY_LINESTATE = ...
SERVER_NOTIFY_MODEMSTATE = ...
SERVER_FLOWCONTROL_SUSPEND = ...
SERVER_FLOWCONTROL_RESUME = ...
SERVER_SET_LINESTATE_MASK = ...
SERVER_SET_MODEMSTATE_MASK = ...
SERVER_PURGE_DATA = ...
RFC2217_ANSWER_MAP = ...
SET_CONTROL_REQ_FLOW_SETTING = ...
SET_CONTROL_USE_NO_FLOW_CONTROL = ...
SET_CONTROL_USE_SW_FLOW_CONTROL = ...
SET_CONTROL_USE_HW_FLOW_CONTROL = ...
SET_CONTROL_REQ_BREAK_STATE = ...
SET_CONTROL_BREAK_ON = ...
SET_CONTROL_BREAK_OFF = ...
SET_CONTROL_REQ_DTR = ...
SET_CONTROL_DTR_ON = ...
SET_CONTROL_DTR_OFF = ...
SET_CONTROL_REQ_RTS = ...
SET_CONTROL_RTS_ON = ...
SET_CONTROL_RTS_OFF = ...
SET_CONTROL_REQ_FLOW_SETTING_IN = ...
SET_CONTROL_USE_NO_FLOW_CONTROL_IN = ...
SET_CONTROL_USE_SW_FLOW_CONTOL_IN = ...
SET_CONTROL_USE_HW_FLOW_CONTOL_IN = ...
SET_CONTROL_USE_DCD_FLOW_CONTROL = ...
SET_CONTROL_USE_DTR_FLOW_CONTROL = ...
SET_CONTROL_USE_DSR_FLOW_CONTROL = ...
LINESTATE_MASK_TIMEOUT = ...
LINESTATE_MASK_SHIFTREG_EMPTY = ...
LINESTATE_MASK_TRANSREG_EMPTY = ...
LINESTATE_MASK_BREAK_DETECT = ...
LINESTATE_MASK_FRAMING_ERROR = ...
LINESTATE_MASK_PARTIY_ERROR = ...
LINESTATE_MASK_OVERRUN_ERROR = ...
LINESTATE_MASK_DATA_READY = ...
MODEMSTATE_MASK_CD = ...
MODEMSTATE_MASK_RI = ...
MODEMSTATE_MASK_DSR = ...
MODEMSTATE_MASK_CTS = ...
MODEMSTATE_MASK_CD_CHANGE = ...
MODEMSTATE_MASK_RI_CHANGE = ...
MODEMSTATE_MASK_DSR_CHANGE = ...
MODEMSTATE_MASK_CTS_CHANGE = ...
PURGE_RECEIVE_BUFFER = ...
PURGE_TRANSMIT_BUFFER = ...
PURGE_BOTH_BUFFERS = ...
RFC2217_PARITY_MAP = ...
RFC2217_REVERSE_PARITY_MAP = ...
RFC2217_STOPBIT_MAP = ...
RFC2217_REVERSE_STOPBIT_MAP = ...
M_NORMAL = ...
M_IAC_SEEN = ...
M_NEGOTIATE = ...
REQUESTED = ...
ACTIVE = ...
INACTIVE = ...
REALLY_INACTIVE = ...
class TelnetOption:
    """Manage a single telnet option, keeps track of DO/DONT WILL/WONT."""
    def __init__(self, connection, name, option, send_yes, send_no, ack_yes, ack_no, initial_state, activation_callback=...) -> None:
        """\
        Initialize option.
        :param connection: connection used to transmit answers
        :param name: a readable name for debug outputs
        :param send_yes: what to send when option is to be enabled.
        :param send_no: what to send when option is to be disabled.
        :param ack_yes: what to expect when remote agrees on option.
        :param ack_no: what to expect when remote disagrees on option.
        :param initial_state: options initialized with REQUESTED are tried to
            be enabled on startup. use INACTIVE for all others.
        """
        ...
    
    def __repr__(self): # -> str:
        """String for debug outputs"""
        ...
    
    def process_incoming(self, command): # -> None:
        """\
        A DO/DONT/WILL/WONT was received for this option, update state and
        answer when needed.
        """
        ...
    


class TelnetSubnegotiation:
    """\
    A object to handle subnegotiation of options. In this case actually
    sub-sub options for RFC 2217. It is used to track com port options.
    """
    def __init__(self, connection, name, option, ack_option=...) -> None:
        ...
    
    def __repr__(self): # -> str:
        """String for debug outputs."""
        ...
    
    def set(self, value): # -> None:
        """\
        Request a change of the value. a request is sent to the server. if
        the client needs to know if the change is performed he has to check the
        state of this object.
        """
        ...
    
    def is_ready(self): # -> bool:
        """\
        Check if answer from server has been received. when server rejects
        the change, raise a ValueError.
        """
        ...
    
    active = ...
    def wait(self, timeout=...): # -> None:
        """\
        Wait until the subnegotiation has been acknowledged or timeout. It
        can also throw a value error when the answer from the server does not
        match the value sent.
        """
        ...
    
    def check_answer(self, suboption): # -> None:
        """\
        Check an incoming subnegotiation block. The parameter already has
        cut off the header like sub option number and com port option value.
        """
        ...
    


class Serial(SerialBase):
    """Serial port implementation for RFC 2217 remote serial ports."""
    BAUDRATES = ...
    def __init__(self, *args, **kwargs) -> None:
        ...
    
    def open(self): # -> None:
        """\
        Open port with current settings. This may throw a SerialException
        if the port cannot be opened.
        """
        ...
    
    def close(self): # -> None:
        """Close port"""
        ...
    
    def from_url(self, url): # -> tuple[Unknown | str | None, Unknown | int | None]:
        """\
        extract host and port from an URL string, other settings are extracted
        an stored in instance
        """
        ...
    
    @property
    def in_waiting(self): # -> int:
        """Return the number of bytes currently in the input buffer."""
        ...
    
    def read(self, size=...): # -> bytes:
        """\
        Read size bytes from the serial port. If a timeout is set it may
        return less characters as requested. With no timeout it will block
        until the requested number of bytes is read.
        """
        ...
    
    def write(self, data): # -> int:
        """\
        Output the given byte string over the serial port. Can block if the
        connection is blocked. May raise SerialException if the connection is
        closed.
        """
        ...
    
    def reset_input_buffer(self): # -> None:
        """Clear input buffer, discarding all that is in the buffer."""
        ...
    
    def reset_output_buffer(self): # -> None:
        """\
        Clear output buffer, aborting the current output and
        discarding all that is in the buffer.
        """
        ...
    
    @property
    def cts(self): # -> bool:
        """Read terminal status line: Clear To Send."""
        ...
    
    @property
    def dsr(self): # -> bool:
        """Read terminal status line: Data Set Ready."""
        ...
    
    @property
    def ri(self): # -> bool:
        """Read terminal status line: Ring Indicator."""
        ...
    
    @property
    def cd(self): # -> bool:
        """Read terminal status line: Carrier Detect."""
        ...
    
    def telnet_send_option(self, action, option): # -> None:
        """Send DO, DONT, WILL, WONT."""
        ...
    
    def rfc2217_send_subnegotiation(self, option, value=...): # -> None:
        """Subnegotiation of RFC2217 parameters."""
        ...
    
    def rfc2217_send_purge(self, value): # -> None:
        """\
        Send purge request to the remote.
        (PURGE_RECEIVE_BUFFER / PURGE_TRANSMIT_BUFFER / PURGE_BOTH_BUFFERS)
        """
        ...
    
    def rfc2217_set_control(self, value): # -> None:
        """transmit change of control line to remote"""
        ...
    
    def rfc2217_flow_server_ready(self): # -> None:
        """\
        check if server is ready to receive data. block for some time when
        not.
        """
        ...
    
    def get_modem_state(self): # -> int:
        """\
        get last modem state (cached value. If value is "old", request a new
        one. This cache helps that we don't issue to many requests when e.g. all
        status lines, one after the other is queried by the user (CTS, DSR
        etc.)
        """
        ...
    


class PortManager:
    """\
    This class manages the state of Telnet and RFC 2217. It needs a serial
    instance and a connection to work with. Connection is expected to implement
    a (thread safe) write function, that writes the string to the network.
    """
    def __init__(self, serial_port, connection, logger=...) -> None:
        ...
    
    def telnet_send_option(self, action, option): # -> None:
        """Send DO, DONT, WILL, WONT."""
        ...
    
    def rfc2217_send_subnegotiation(self, option, value=...): # -> None:
        """Subnegotiation of RFC 2217 parameters."""
        ...
    
    def check_modem_lines(self, force_notification=...): # -> None:
        """\
        read control lines from serial port and compare the last value sent to remote.
        send updates on changes.
        """
        ...
    
    def escape(self, data): # -> Generator[Unknown | Literal[b'ÿ'], None, None]:
        """\
        This generator function is for the user. All outgoing data has to be
        properly escaped, so that no IAC character in the data stream messes up
        the Telnet state machine in the server.

        socket.sendall(escape(data))
        """
        ...
    
    def filter(self, data): # -> Generator[Unknown, None, None]:
        """\
        Handle a bunch of incoming bytes. This is a generator. It will yield
        all characters not of interest for Telnet/RFC 2217.

        The idea is that the reader thread pushes data from the socket through
        this filter:

        for byte in filter(socket.recv(1024)):
            # do things like CR/LF conversion/whatever
            # and write data to the serial port
            serial.write(byte)

        (socket error handling code left as exercise for the reader)
        """
        ...
    


if __name__ == '__main__':
    s = ...
